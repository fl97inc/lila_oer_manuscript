numpy>=1.26
pandas>=2.0
plotly>=5.20
fastparquet>=2023.1
kaleido>=0.2,<1.0
scipy>=1.11
matplotlib>=3.8
# cairosvg (used by fig_5a/fig_5b to rasterise SVG -> PNG) needs the native
# cairo C library, which pip cannot install. See README "Setup" for the
# per-OS system package (e.g. `brew install cairo`).
cairosvg>=2.7
pillow>=10.0
